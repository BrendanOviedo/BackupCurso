import random
random.choice(["Head", "Tail"]) #Elige aleatoriamente un valor en la lista sea cual sea el valor str, int
random.randint(1, 10) #Entrega un numero aleatorio entre los dos valores dados incluyendolos
cards = ["Jack", "Queen", "King"]
random.shuffle(cards)
import statistics
